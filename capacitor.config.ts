import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-firebase-prak-pemob',
  webDir: 'dist'
};

export default config;
